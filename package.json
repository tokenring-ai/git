{
  "name": "@tokenring-ai/git",
  "description": "Version control tools for commits, branch management, and automated rollbacks.",
  "version": "0.2.0",
  "dependencies": {
    "@tokenring-ai/ai-client": "0.2.0",
    "@tokenring-ai/app": "0.2.0",
    "@tokenring-ai/chat": "0.2.0",
    "@tokenring-ai/agent": "0.2.0",
    "@tokenring-ai/filesystem": "0.2.0",
    "@tokenring-ai/lifecycle": "0.2.0",
    "@tokenring-ai/testing": "0.2.0",
    "@tokenring-ai/utility": "0.2.0",
    "@tokenring-ai/terminal": "0.2.0",
    "execa": "^9.6.1",
    "zod": "^4.3.6"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "type": "module",
  "license": "MIT",
  "scripts": {
    "eslint": "eslint --fix ./",
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^4.1.1",
    "typescript": "^6.0.2"
  }
}
